SESSION_SECRET=changeme_use_a_long_random_string
RACK_ENV=development

# Directory for the JSON file store (businesses, clients, invoices, timesheets).
# Defaults to ./data if unset.
# DATA_DIR=./data
